<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.incrementa.appincrementa" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".Main"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Menu"
            android:label="@string/title_activity_Menu" >
        </activity>

        <activity android:name=".Registrarse"
            android:label="@string/title_activity_Registrarse" >
        </activity>

        <activity android:name=".ReportesClarity"
            android:label="@string/title_activity_ReportesClarity" >
        </activity>



    </application>

</manifest>
